#include <bits/stdc++.h>
#include <ctype.h>
using namespace std;

int main(void){
    string s;
    cin>>s;
    for(int i=0;i<s.size();i++){
        if(s.at(i) >= 'a' && s.at(i) <= 'z'){
            int a;
        }
        else{
            cout<<i+1<<endl;return 0;
        }
    }
}